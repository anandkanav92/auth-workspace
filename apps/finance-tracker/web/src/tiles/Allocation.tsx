import { TileCard } from "./TileCard";
import type { TileProps } from "./types";

// Placeholder — full ECharts donut + look-through implementation lands in 11.2.
export function Allocation(_props: TileProps) {
  return <TileCard title="Allocation">{null}</TileCard>;
}
