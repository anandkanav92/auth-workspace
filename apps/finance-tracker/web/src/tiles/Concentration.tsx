import { TileCard } from "./TileCard";
import type { TileProps } from "./types";

// Placeholder — full top-5 bars implementation lands in 11.3.
export function Concentration(_props: TileProps) {
  return <TileCard title="Top 5">{null}</TileCard>;
}
