import { TileCard } from "./TileCard";
import type { TileProps } from "./types";

// Placeholder — Effective-N + sub-scores implementation lands in 11.4.
export function DiversificationScore(_props: TileProps) {
  return <TileCard title="Diversification">{null}</TileCard>;
}
