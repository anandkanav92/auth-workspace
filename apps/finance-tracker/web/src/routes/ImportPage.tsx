/**
 * Import placeholder (M10.4 route `/import`). The drag-and-drop upload + preview
 * + commit flow arrives in M12.
 */
export function ImportPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-semibold tracking-tight text-fg">Import</h1>
      <p className="text-sm text-muted">
        Upload a broker statement — coming soon.
      </p>
    </div>
  );
}
