/**
 * Settings placeholder (M10.4 route `/settings`). Real settings (accounts,
 * preferences, data export) arrive in later milestones.
 */
export function SettingsPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-semibold tracking-tight text-fg">Settings</h1>
      <p className="text-sm text-muted">Settings — coming soon.</p>
    </div>
  );
}
