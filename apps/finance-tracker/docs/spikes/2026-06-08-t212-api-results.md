# Spike 0 — Trading 212 Public API contract (validated live)

**Date:** 2026-06-08 · **Account:** General Invest (base currency EUR) · validated from the Mac Mini (key IP-locked to `77.173.30.177`). No key or PII committed — schemas only.

## Auth — HTTP Basic
`Authorization: Basic base64("<publicKey>:<privateKey>")`. Public key = username, private key = password. Confirmed HTTP 200. Base URL **live**: `https://live.trading212.com`; demo: `https://demo.trading212.com`.

> **Design impact:** the connect endpoint + Settings UI must take **two** fields (public + private). Store `encrypt("<public>:<private>")` in `broker_connections.api_key_enc`; the provider base64-encodes the decrypted string for the `Basic` header.

## Rate limits (per endpoint — STRICT)
| Endpoint | limit | period |
|---|---|---|
| `/api/v0/equity/account/info` | 1 | 30s |
| `/api/v0/equity/portfolio` | 1 | 5s |
| `/api/v0/equity/history/orders` | 6 | 60s |
| `/api/v0/history/dividends` | 6 | 60s |

Headers returned: `x-ratelimit-limit/period/remaining/reset/used`. → sync must throttle (≥ period between calls), honor `429` + `x-ratelimit-reset`, and page history slowly (≤6/min). A full backfill of a long history may take several minutes — do it once, then incremental.

## Schemas (field → type)

**`GET /equity/account/info`** → `{ id:int, currencyCode:str }`  (account id + base currency)

**`GET /equity/portfolio`** → array of:
`{ ticker:str (T212 fmt e.g. AAPL_US_EQ), quantity:float, averagePrice:float (instrument ccy), currentPrice:float, ppl:float, fxPpl:float, initialFillDate:str(ISO), frontend:str, maxBuy:float, maxSell:?, pieQuantity:float }`
- **No ISIN / no instrument currency here.** Resolve via the order ledger (below) or `/equity/metadata/instruments`.
- `averagePrice` is per-share in the **instrument** currency → cost_basis = quantity × averagePrice; **GBX must be normalized** (reuse `normalizePence`).

**`GET /equity/history/orders?limit=&cursor(nextPagePath)`** → `{ items:[ { order:{ id:int, type:str, ticker:str, status:str, value:float, filledValue:float, currency:str, side:str(BUY/SELL), createdAt:str, instrument:{ ticker:str, name:str, isin:str, currency:str } }, fill:{ id:int, quantity:float, price:float, filledAt:str, walletImpact:{ currency:str, netValue:float, fxRate:float, taxes:[{name,quantity,currency,chargedAt}] } } } ], nextPagePath:str }`
- Ledger row: `external_id = order.id`, `type = side.toLowerCase()`, `ticker/isin/currency = order.instrument.*`, `quantity = fill.quantity`, `price = fill.price`, `fee = sum(fill.walletImpact.taxes[].quantity)`, `occurred_at = fill.filledAt`, `fxRate = fill.walletImpact.fxRate`.

**`GET /history/dividends?limit=&cursor(nextPagePath)`** → `{ items:[ { ticker:str, instrument:{ticker,name,isin,currency}, reference:str, quantity:float, amount:float, currency:str, grossAmountPerShare:float, amountInEuro:float, paidOn:str, type:str } ], nextPagePath:str }`
- Ledger row: `external_id = reference`, `type = 'dividend'`, `ticker/isin = instrument.*`, `amount`/`amountInEuro`, `occurred_at = paidOn`.

## Mapping to our model
- Build a `ticker → { isin, currency, name }` map from the orders history (covers every held ticker, since holdings originate from buys). Fallback: `/equity/metadata/instruments`.
- For each portfolio position: derive broker symbol from the T212 ticker (`AAPL_US_EQ` → `AAPL`), then `resolveTicker(isin, brokerSym, normalizedCurrency)` → our Yahoo ticker for pricing (reuses the fixed resolver incl. GBX/multi-listing). `holdings.cost_basis = qty × averagePrice` (GBX-normalized), `cost_currency` = normalized instrument currency.
- Pagination: follow `nextPagePath` until null. Dedup ledger by `(user, source, external_id)`.

## Pagination cursor
`nextPagePath` is a relative path to the next page (append to base). Stop when absent/null.

## Open items for M2
- Confirm `nextPagePath` exact format (relative vs query) on first real paginate.
- `/equity/metadata/instruments` is large + rate-limited; prefer order-derived ticker map.
